		<div class="actions">
			<p>
				<%= link_to "Back to Order", order_path(@order), :class => "button button-okay" %>
			</p>
		</div>

		<section>
			<h2>Editing: <%= @order.name %></h2>
			<article class="form">
<%= render 'form' %>
			</article>
		</section>